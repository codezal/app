Click an element in the headless preview browser. `target` = a ref number from browser_snapshot (e.g. "12") or a raw CSS selector. The element is scrolled into view first. Returns a fresh snapshot so you can see the resulting page state.

Call browser_navigate then browser_snapshot first to obtain refs. If the click triggers async loading, follow with browser_wait before acting again.
